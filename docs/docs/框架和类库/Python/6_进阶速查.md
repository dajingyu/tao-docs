# 6. 进阶速查

> 入门稳定后再深挖；此处便于「听说过 → 能查到关键词」。

## 6.1 列表 / 字典 / 集合推导式

```python
squares = [x * x for x in range(10)]
evens = [x for x in nums if x % 2 == 0]
mapping = {k: v * 2 for k, v in d.items()}
unique = {x.lower() for x in words}
```

- 可读性优先；复杂逻辑用普通 `for` 循环。

## 6.2 `match / case`（3.10+）

```python
match command:
    case "quit":
        break
    case ["load", path]:
        load(path)
    case {"name": name, "age": age}:
        print(name, age)
    case _:
        print("unknown")
```

## 6.3 装饰器（初识）

```python
def log(func):
    def wrapper(*args, **kwargs):
        print("call", func.__name__)
        return func(*args, **kwargs)
    return wrapper

@log
def add(a, b):
    return a + b
```

- 本质：**函数接收函数，返回新函数**；常用于日志、权限、缓存。

## 6.4 生成器 `yield`

```python
def read_lines(path):
    with open(path, encoding="utf-8") as f:
        for line in f:
            yield line.rstrip()

for line in read_lines("big.log"):
    process(line)
```

- **惰性求值**，适合大文件、大数据流。

## 6.5 类型注解与 `typing`

```python
from typing import Optional, List, Dict, Callable

def find_user(id: int) -> Optional[dict]:
    ...

def apply(fn: Callable[[int], int], x: int) -> int:
    return fn(x)

names: List[str] = []
scores: Dict[str, int] = {}
```

- 3.9+ 可用内置泛型：`list[str]`、`dict[str, int]`。

## 6.6 异步（了解）

```python
import asyncio

async def fetch():
    await asyncio.sleep(1)
    return "done"

asyncio.run(fetch())
```

- Web / 爬虫高并发时再系统学；初学先掌握同步代码。

## 6.7 常用标准库索引

| 模块 | 用途 |
| --- | --- |
| `datetime` | 日期时间 |
| `pathlib` | 路径 |
| `json` | JSON |
| `re` | 正则 |
| `random` | 随机 |
| `collections` | `Counter`, `defaultdict`, `deque` |
| `itertools` | 迭代工具 |
| `functools` | `lru_cache` 等 |
| `dataclasses` | 轻量数据类 |
| `enum` | 枚举 |

```python
from datetime import datetime, timedelta
now = datetime.now()
tomorrow = now + timedelta(days=1)

from dataclasses import dataclass

@dataclass
class Point:
    x: float
    y: float
```

## 6.8 测试入门

```python
# test_add.py
def add(a, b):
    return a + b

def test_add():
    assert add(1, 2) == 3
```

```bash
pip install pytest
pytest
```

## 6.9 代码风格

- 遵循 **PEP 8**：4 空格缩进、行宽约 88（Black 默认）/ 79（PEP）。
- 格式化工具：`black`；import 排序：`isort`；检查：`ruff` / `flake8`。

## 6.10 下一步学习方向（自选）

| 方向 | 典型库 / 主题 |
| --- | --- |
| Web | FastAPI / Flask |
| 爬虫 | requests + BeautifulSoup |
| 数据分析 | pandas + numpy |
| 自动化 | 脚本、openpyxl |
| AI / 脚本 | 调用 API、本地小工具 |

学完可在本目录新建 `7_xxx.md` 并链回 [index](./index.md)。
